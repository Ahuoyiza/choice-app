import React from 'react';
import{ 
    Flex,
    Box,
    Text,
    Heading,
    Image,
  } from '@chakra-ui/react';

const Layout = () => {
    return (
        <Flex>
            <Heading>Layout</Heading>
        </Flex>
    )
}

export default Layout
